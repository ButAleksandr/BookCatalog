@model string[]

var authorModalSettings = {
    getAuthorUrl: '@Url.Action("Get", "Author")',
    saveAuthorUrl: '@Url.Action("Save", "Author")',
    showAfterInit: @Model[0],
    withCreate: @Model[1],
    TargetTable: @Model[2]
};

AuthorModal.Initialize(null, authorModalSettings);